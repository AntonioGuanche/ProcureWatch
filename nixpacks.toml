# Railway build config: Python API + React frontend
[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "cd web && npm ci",
]

[phases.build]
cmds = ["cd web && npm run build"]

[start]
cmd = "bash scripts/start.sh"
